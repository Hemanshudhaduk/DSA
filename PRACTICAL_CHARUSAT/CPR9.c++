#include <iostream>
#include <queue>
using namespace std;
void Ticket(int arr[] ,int N)
{

    for(int i=0;i<N;i++){
        
    }
}  
int main()
{
    int N;
    int a[MAX_PATH];
    cout<<"Enter a size : ";
    cin>>N;
    cout<<"Enter a Array Element : ";
    for(int i=0;i<N;i++){
        cin>>a[i];
    }
    Ticket(a,N);
}